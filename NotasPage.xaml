<Page x:Class="ProjetoWPF.NotasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjetoWPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#F5F5F5">
    <Page.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </Page.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Cabeçalho -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="10" 
                Padding="20"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Histórico de Notas Fiscais" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"/>
                <TextBlock Text="Gerencie e visualize todas as notas fiscais criadas" 
                           FontSize="14" 
                           Foreground="#7F8C8D"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Filtros -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="10" 
                Padding="20"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Filtros" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#34495E"
                           Margin="0,0,0,10"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="De:" 
                                   VerticalAlignment="Center" 
                                   Margin="0,0,5,0"
                                   FontWeight="SemiBold"/>
                        <DatePicker x:Name="dpDataInicio" 
                                    Style="{StaticResource DatePickerStyle}"
                                    Width="150"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="Até:" 
                                   VerticalAlignment="Center" 
                                   Margin="15,0,5,0"
                                   FontWeight="SemiBold"/>
                        <DatePicker x:Name="dpDataFim" 
                                    Style="{StaticResource DatePickerStyle}"
                                    Width="150"/>
                    </StackPanel>
                    
                    <ComboBox x:Name="cmbTipo" 
                              Grid.Column="2"
                              Width="150"
                              Margin="15,0,5,0"
                              Padding="8"
                              FontSize="13">
                        <ComboBoxItem Content="Todos os Tipos" IsSelected="True"/>
                        <ComboBoxItem Content="Ternos"/>
                        <ComboBoxItem Content="Vestidos"/>
                    </ComboBox>
                    
                    <Button Content="Filtrar" 
                            Grid.Column="3"
                            x:Name="btnFiltrar"
                            Style="{StaticResource FilterButtonStyle}"/>
                    
                    <Button Content="Limpar" 
                            Grid.Column="4"
                            x:Name="btnLimparFiltro"
                            Style="{StaticResource FilterButtonStyle}"
                            Background="#95A5A6"/>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Lista de Notas -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="10" 
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Notas Encontradas: " 
                               FontSize="16" 
                               FontWeight="Bold"
                               Foreground="#34495E"/>
                    <TextBlock x:Name="txtTotalNotas" 
                               Text="0" 
                               FontSize="16" 
                               FontWeight="Bold"
                               Foreground="#3498DB"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <DataGrid x:Name="dgNotas" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeight="40"
                              Background="White"
                              BorderThickness="0"
                              AlternatingRowBackground="#F8F9FA">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nº Nota" 
                                                Binding="{Binding NumeroNota}" 
                                                Width="100"
                                                FontWeight="Bold"/>
                            
                            <DataGridTextColumn Header="Data" 
                                                Binding="{Binding Data, StringFormat='dd/MM/yyyy'}" 
                                                Width="100"/>
                            
                            <DataGridTextColumn Header="Tipo" 
                                                Binding="{Binding Tipo}" 
                                                Width="100"/>
                            
                            <DataGridTextColumn Header="Cliente" 
                                                Binding="{Binding NomeCliente}" 
                                                Width="*"
                                                MinWidth="150"/>
                            
                            <DataGridTextColumn Header="Produto" 
                                                Binding="{Binding Produto}" 
                                                Width="*"
                                                MinWidth="150"/>
                            
                            <DataGridTextColumn Header="Valor Total" 
                                                Binding="{Binding ValorTotal, StringFormat='R$ {0:N2}'}" 
                                                Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#27AE60"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTemplateColumn Header="Ações" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="" 
                                                    ToolTip="Visualizar"
                                                    Margin="2"
                                                    Padding="8,4"
                                                    Background="#3498DB"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="3" 
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            
                                            <Button Content="" 
                                                    ToolTip="Imprimir"
                                                    Margin="2"
                                                    Padding="8,4"
                                                    Background="#9B59B6"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="3" 
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            
                                            <Button Content="" 
                                                    ToolTip="Excluir"
                                                    Margin="2"
                                                    Padding="8,4"
                                                    Background="#E74C3C"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="3" 
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
